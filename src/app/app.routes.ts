import { Routes } from '@angular/router';
import { UserMain } from './user/component/user-main/user-main';

export const routes: Routes = [
    { 
        path: '', 
        component: UserMain 
    }
];
